import React from 'react'

const Prop = (props) => {
  return (<>
  <div>Prop</div>
    <div>hi this is sumesh{props.name}</div>
    <h3>ok all good{props.name}</h3>
  </>
    
  )
}

export default Prop